{
  "name": "lore-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cross-env $(cat .env | grep -v '^#' | xargs) cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
